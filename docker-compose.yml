services:
  backend:
    build: ./backend1
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: file:/app/data/dev.db
      JWT_SECRET: what
      PORT: "5000"
    volumes:
      - sqlite_stuff:/app/data

volumes:
  sqlite_stuff:
